interface:
  display_name: "Plugin Creator"
  short_description: "Build GitHub-hosted plugin marketplaces"
  default_prompt: "Use $plugin-creator to package a skill as a repository-managed plugin, register it in .agents/plugins/marketplace.json, validate the repository, and prepare it for ChatGPT marketplace import."
